# TBA -- script to copy agent files from this repo to user profile appropriately for each type of agent. This is a one-time setup script that can be run after cloning and aliasing this repo to initialize the agent files in the user's environment.
# It should follow the pattern outlined by `../docs/idealized-setup/README.md` to copy the canonical `..template/.agents` files to `$env:USERPROFILE\.agents`, and then symlink to them in individual agent root directories as described.  It should also handle any necessary directory creation and ensure that existing files are not overwritten without confirmation.
# Specifically regarding the `AGENTS.md` file, it should prompt the user to replace any `<*_PREFERENCE>` placeholders (described in the YAML frontmatter `preference_blocks`) with their actual preferences, and ideally provide an interactive way to set those preferences during the setup process.  This could be done through a series of prompts or a simple configuration interface within the script.  After setup, remove the frontmatter from the `AGENTS.md` file to prevent confusion and ensure that the file is ready for use by the agents without any extraneous information.
# Handle Claude vs Copilot differences in agent file templates, ensuring that the correct files are copied and configured for all chosen agent frameworks.